<table>
    {% for item in results %}
    <TR>
        <TD ><span>{{item.index}}</span></TD>
        <TD ><a href="{{ item.alignment_url }}" target="_blank">{{item.instance_id}}</a></TD>
        <TD ><span>{{item.score}}</span></TD>
    </TR>
    {% endfor %}
</table>